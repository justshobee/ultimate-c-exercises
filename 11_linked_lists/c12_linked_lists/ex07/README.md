## 07. ft_list_at
 
* **Description:** Create the function `ft_list_at`, which returns the Nth element of the list, knowing that the first element of the list is when `nbr` equals `0`. In case of error, it should return a null pointer.
* **Directory:** `ex07/`
* **Files to submit:** `ft_list_at.c`, `ft_list.h`
* **Authorized:** None
* **Function Prototype:** `t_list *ft_list_at(t_list *begin_list, unsigned int nbr);`
