## 04. ft_list_push_back
 
* **Description:** Create the function `ft_list_push_back`, which adds a new element of `t_list` type at the end of the list. It should assign `data` to the given argument, and update the pointer at the beginning of the list if necessary.
* **Directory:** `ex04/`
* **Files to submit:** `ft_list_push_back.c`, `ft_list.h`
* **Authorized:** `ft_create_elem`
* **Function Prototype:** `void ft_list_push_back(t_list **begin_list, void *data);`
