## 00. ft_create_elem
 
* **Description:** Create the function `ft_create_elem`, which creates a new element of `t_list` type. It should assign `data` to the given argument and `next` to `NULL`.
* **Directory:** `ex00/`
* **Files to submit:** `ft_create_elem.c`, `ft_list.h`
* **Authorized:** `malloc`
* **Function Prototype:** `t_list *ft_create_elem(void *data);`
