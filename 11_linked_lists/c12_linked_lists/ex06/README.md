## 06. ft_list_clear
 
* **Description:** Create the function `ft_list_clear`, which removes and frees all links from the list. `free_fct` is used to free each element's data.
* **Directory:** `ex06/`
* **Files to submit:** `ft_list_clear.c`, `ft_list.h`
* **Authorized:** `free`
* **Function Prototype:** `void ft_list_clear(t_list *begin_list, void (*free_fct)(void *));`
