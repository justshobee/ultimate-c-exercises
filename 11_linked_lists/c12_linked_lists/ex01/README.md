## 01. ft_list_push_front
 
* **Description:** Create the function `ft_list_push_front`, which adds a new element of type `t_list` to the beginning of the list. It should assign `data` to the given argument, and update the pointer at the beginning of the list if necessary.
* **Directory:** `ex01/`
* **Files to submit:** `ft_list_push_front.c`, `ft_list.h`
* **Authorized:** `ft_create_elem`
* **Function Prototype:** `void ft_list_push_front(t_list **begin_list, void *data);`
